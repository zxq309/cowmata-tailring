# Security policy

Report security issues privately to the repository maintainer. Do not open public issues containing credentials, farm locations, device identifiers, cow-level raw data or private download links.

If a credential is pasted into chat, source code, logs or an issue, rotate it immediately and remove it from all reachable history. GitHub authentication must use OAuth, SSH or a personal access token; account passwords are never stored in this repository.
