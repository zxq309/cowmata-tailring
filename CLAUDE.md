# Claude project context

Read `AGENTS.md` first, then `README.md`, `docs/DATA_CONTRACT.md` and `docs/VERIFICATION_20260818.md`.

The full supervised cache is intentionally absent from Git. Use `examples/demo_data/` for executable inspection. Do not infer production metrics from the 60-second demo or from overlapping windows. Treat `weights/deploy/gbdt_full.joblib` as the current deploy model and `weights/checkpoints/offline_tcn_dev_epoch2.pt` as a development checkpoint.
