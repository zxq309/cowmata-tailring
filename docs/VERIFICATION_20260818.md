# 20260818 基线验证记录

验证日期：2026-08-18。验证对象为新建 `20260818` 目录，源对照为 `20260816/20260816`。

## 结论

- 工程可编辑安装，删除 SSL 支线后的 24 项当前合同测试全部通过。
- 监督数据全量预检通过：逐一读取 132 个缓存会话（131.5739 小时），未发现形状错误或非有限值；344,287 个监督样本和 6 个严格 LOCO 折无会话交叉。
- 确认现有 TCN 检查点的 `ssl_transfer=null`后，删除 2.6527 GB `ssl_cache`、19.897 MB `ssl_index` 及 8 个孤立代码文件。
- GBDT 正式权重和源文件 SHA-256 一致。在真实缓存会话上，新入口产生 7,159 个 2 Hz 预测点和 3 个事件候选。
- 将新结果与 `20260816` 旧入口逐任务比较：`center_index` 和 `center_time_ms` 完全一致，8 个任务最大概率绝对差为 `1.1102230246251565e-16`。
- offline TCN 开发检查点已对真实缓存运行 4 个窗口：姿态输出形状为 `[4, 2]`，行走输出形状为 `[4]`，数值全部有限，姿态概率每行和为 1。
- 仓库内置 `demo_session_60s` 真实样例输出 120 个 2 Hz 点，GBDT 与 TCN 均能从该样例完成推理。
- 首轮清理后 Git 纳入 62 个算法和数据契约文件、16.11 MB，最大单文件为 8.55 MB；后续增加的仓库治理文件均为小型文本。生成式 `dist/` 已删除，不作为版本来源。
- CPU 环境前向、反向、优化器更新烟雾测试通过。本机可见 NVIDIA GeForce RTX 2080 Ti，但本次隔离验证环境安装的是 CPU PyTorch，因此未在本记录中声称 CUDA 训练烟雾测试已通过。

## 数据事件概况

| 事件 | 合格事件 | 牛数 |
|---|---:|---:|
| STANDING_UP | 71 | 5 |
| LYING_DOWN | 60 | 4 |
| URINATION | 87 | 5 |
| DEFECATION | 15 | 4 |
| TAIL_RAISED | 26 | 4 |
| TAIL_WAGGING | 4 | 1 |

## 必须保留的限制

- 6 个 LOCO 折中只有 4 个折的测试样本不少于 1,000；小折结果只能汇总解读。
- 尚无完整盲看时间覆盖，因此当前不能声称事件 Precision 或误报/牛/24 h 已经得到可验收证明。
- 现有 Joblib 包加载时 XGBoost 会提示旧版序列化警告；在本次 XGBoost 3.4.1 验证中加载与预测均成功，且与旧入口数值一致。
